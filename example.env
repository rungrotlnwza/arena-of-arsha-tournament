PORT=3000
LIVERELOAD=true

# Database (when using mysql2)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=mydb

# JWT (when using jsonwebtoken)
JWT_SECRET=your-secret-key

# Upload (when using multer) – optional

<!-- Success Section -->
<section class="py-5" style="min-height: 80vh; display: flex; align-items: center; justify-content: center;">
    <div class="container text-center">
        <div class="success-card mx-auto" style="max-width: 600px; background: var(--card-bg); border: 2px solid var(--primary-gold); border-radius: 25px; padding: 60px 40px;">
            <i class="bi bi-check-circle-fill" style="font-size: 5rem; color: var(--primary-gold);"></i>
            <h2 class="mt-4 mb-3" style="color: var(--primary-gold); font-weight: 700;">สมัครสำเร็จ!</h2>

            <div class="mb-4 p-3" style="background: rgba(255,215,0,0.1); border-radius: 10px;">
                <div>
                    <small style="color: #888;">ชื่อทีม</small>
                    <div style="color: #fff; font-weight: 600; font-size: 1.3rem;" id="teamName">-</div>
                </div>
            </div>

            <p class="mb-4" id="successMessage" style="color: #ccc; font-size: 1.1rem;">
                ทีมของคุณได้ลงทะเบียนเข้าร่วมการแข่งขันเรียบร้อยแล้ว<br>
                โปรดติดตามประกาศรายชื่อทีมและอาชีพที่สุ่มได้ในภายหลัง
            </p>

            <div class="alert alert-warning mb-4" style="background: rgba(255,193,7,0.1); border-color: var(--primary-gold); color: #ccc;">
                <i class="bi bi-info-circle-fill me-2" style="color: var(--primary-gold);"></i>
                <strong style="color: var(--primary-gold);">สำคัญ:</strong>
                กรุณาเข้าร่วม Discord เพื่อติดตามข่าวสารและประกาศสำคัญ
            </div>

            <div class="d-flex flex-wrap justify-content-center gap-3">
                <a href="https://discord.gg/Zxjj8FCRe9" target="_blank" class="btn btn-custom" style="background: #5865F2; border-color: #5865F2;">
                    <i class="bi bi-discord me-2"></i>เข้าร่วม Discord
                </a>
                <a href="/" class="btn btn-outline-custom">
                    <i class="bi bi-house-fill me-2"></i>กลับหน้าหลัก
                </a>
            </div>
        </div>
    </div>
</section>

<script>
    const urlParams = new URLSearchParams(window.location.search);
    const team = urlParams.get('team');
    if (team) {
        document.getElementById('teamName').textContent = team;
    }
</script>